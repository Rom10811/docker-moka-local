../odoo:
  remotes:
    origin: https://github.com/odoo/odoo
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    # [FIX] event: badge report dates in event's tz
    - origin refs/pull/83946/head
    # [FIX] event: do not fail if mail.template is missing
    - origin refs/pull/92882/head

design-themes:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/odoo/design-themes
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
