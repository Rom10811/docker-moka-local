<?xml version="1.0" encoding="UTF-8"?>
<odoo>

  <record id="view_calendar_form_inherit" model="ir.ui.view">
    <field name="name">view.calendar.form.inherit</field>
    <field name="model">calendar.event</field>
    <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
    <field name="arch" type="xml">
      <xpath expr="/form/sheet/button[1]" position="after">
        <button string="CRM" class="oe_stat_button float-right" icon="fa-bars" type="object" name="open_linked_crm"
                attrs="{'invisible':[('crm_id', '=', False)]}"/>
        <field name="crm_id" invisible="1"/>
      </xpath>
    </field>
  </record>
</odoo>
